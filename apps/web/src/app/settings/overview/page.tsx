import { SettingsSection } from "@/components/settings/settings-section";
import { OverviewPanel } from "@/components/settings/panels";

export default function SettingsOverviewPage() {
  return (
    <SettingsSection>
      <OverviewPanel />
    </SettingsSection>
  );
}
