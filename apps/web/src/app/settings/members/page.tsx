import { SettingsSection } from "@/components/settings/settings-section";
import { MembersPanel } from "@/components/settings/panels";

export default function SettingsMembersPage() {
  return (
    <SettingsSection>
      <MembersPanel />
    </SettingsSection>
  );
}
